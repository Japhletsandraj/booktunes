-r requirements.txt

pytest==8.2.2
pytest-asyncio==0.23.7
pytest-cov==5.0.0
httpx==0.27.0          # also used by tests via ASGITransport
ruff==0.5.0
aiosqlite==0.20.0      # lets the non-pgvector tests run without Postgres
